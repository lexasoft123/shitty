name: CI

on:
  pull_request:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build Linux
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Build package
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#checks.x86_64-linux.build \
            2>&1 | tee "$RUNNER_TEMP/build.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/build.log" \
            --title "Build Linux" \
            --reproduce "nix build -L --no-link .#checks.x86_64-linux.build"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: build-log
          path: ${{ runner.temp }}/build.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  tests-alpine:
    name: Tests Alpine
    needs: build
    runs-on: ubuntu-24.04
    container: alpine:3.24
    timeout-minutes: 90
    steps:
      - name: Prepare Alpine for GitHub Actions
        run: apk add --no-cache gcompat git

      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install the stock Alpine toolchain
        run: |
          apk add --no-cache \
            bash gcc g++ musl-dev linux-headers make python3 zstd perl perl-doc pkgconf \
            diffutils procps ragel glslang rsvg-convert ncurses-dev \
            brotli-dev fontconfig-dev freetype-dev harfbuzz-dev simdutf-dev \
            libxkbcommon-dev wayland-dev wayland-protocols \
            vulkan-headers vulkan-loader-dev mesa-vulkan-swrast

      - name: Run full test suite with GCC and musl
        env:
          CC: gcc
          CXX: g++
          SHITTY_TEST_VULKAN_REQUIRED: "1"
        run: |
          set -o pipefail
          export VK_DRIVER_FILES="$(find /usr/share/vulkan/icd.d -name 'lvp_icd*.json' -print -quit)"
          export VK_ICD_FILENAMES="$VK_DRIVER_FILES"
          ./build test a so tgz 2>&1 | tee "$RUNNER_TEMP/tests-alpine.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/tests-alpine.log" \
            --title "Tests Alpine" \
            --reproduce "run the Tests Alpine steps from .github/workflows/ci.yml in an alpine:3.24 container"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: tests-alpine-log
          path: ${{ runner.temp }}/tests-alpine.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  tests-fedora:
    name: Tests Fedora
    needs: build
    runs-on: ubuntu-24.04
    container: fedora:44
    timeout-minutes: 90
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install the stock Fedora toolchain
        # Retry: the Fedora mirrors intermittently fail mid-download (curl 23,
        # "Failed writing received data to disk", usually on a zchunk delta), so
        # disable zchunk, prefer the fastest mirror, and try a few times.
        run: |
          for attempt in 1 2 3 4 5; do
            if dnf -y --setopt=install_weak_deps=False \
                --setopt=retries=10 --setopt=timeout=60 \
                --setopt=fastestmirror=True --setopt=zchunk=False \
                install \
                gcc-c++ make git python3 perl pkgconf-pkg-config diffutils procps-ng \
                ragel glslang librsvg2-tools ncurses-devel \
                freetype-devel fontconfig-devel harfbuzz-devel \
                libxkbcommon-devel \
                vulkan-headers vulkan-loader-devel mesa-vulkan-drivers \
                wayland-devel wayland-protocols-devel; then
              exit 0
            fi
            echo "dnf install failed (attempt $attempt); cleaning and retrying" >&2
            dnf clean all || true
            sleep 15
          done
          echo "dnf install failed after 5 attempts" >&2
          exit 1

      - name: Run full test suite with GCC and Ragel 7
        env:
          CC: gcc
          CXX: g++
        run: |
          set -o pipefail
          ./build test a so tgz 2>&1 | tee "$RUNNER_TEMP/tests-fedora.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/tests-fedora.log" \
            --title "Tests Fedora" \
            --reproduce "run the Tests Fedora steps from .github/workflows/ci.yml in a fedora:44 container"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: tests-fedora-log
          path: ${{ runner.temp }}/tests-fedora.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  build-darwin:
    name: Build Darwin
    needs: build
    runs-on: macos-15
    timeout-minutes: 60
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Build flake package
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#checks.aarch64-darwin.build \
            2>&1 | tee "$RUNNER_TEMP/build-darwin.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/build-darwin.log" \
            --title "Build Darwin" \
            --reproduce "nix build -L --no-link .#checks.aarch64-darwin.build"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: build-darwin-log
          path: ${{ runner.temp }}/build-darwin.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  nix-flake-linux:
    name: Nix Linux
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Build flake package
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#packages.x86_64-linux.default \
            2>&1 | tee "$RUNNER_TEMP/nix-flake-linux.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/nix-flake-linux.log" \
            --title "Nix Linux" \
            --reproduce "nix build -L --no-link .#packages.x86_64-linux.default"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: nix-flake-linux-log
          path: ${{ runner.temp }}/nix-flake-linux.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  nix-flake-darwin:
    name: Nix Darwin
    needs: build-darwin
    runs-on: macos-15
    timeout-minutes: 60
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Build flake package
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#packages.aarch64-darwin.default \
            2>&1 | tee "$RUNNER_TEMP/nix-flake-darwin.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/nix-flake-darwin.log" \
            --title "Nix Darwin" \
            --reproduce "nix build -L --no-link .#packages.aarch64-darwin.default"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: nix-flake-darwin-log
          path: ${{ runner.temp }}/nix-flake-darwin.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  tests-darwin-0:
    name: Tests Darwin 0/4
    needs: build-darwin
    runs-on: macos-15
    timeout-minutes: 90
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Run test partition 0/4 on Darwin
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#checks.aarch64-darwin.tests-0-of-4 \
            2>&1 | tee "$RUNNER_TEMP/tests-darwin-0-of-4.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/tests-darwin-0-of-4.log" \
            --title "Tests Darwin 0/4" \
            --reproduce "nix build -L --no-link .#checks.aarch64-darwin.tests-0-of-4"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: tests-darwin-0-of-4-log
          path: ${{ runner.temp }}/tests-darwin-0-of-4.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  tests-darwin-1:
    name: Tests Darwin 1/4
    needs: build-darwin
    runs-on: macos-15
    timeout-minutes: 90
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Run test partition 1/4 on Darwin
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#checks.aarch64-darwin.tests-1-of-4 \
            2>&1 | tee "$RUNNER_TEMP/tests-darwin-1-of-4.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/tests-darwin-1-of-4.log" \
            --title "Tests Darwin 1/4" \
            --reproduce "nix build -L --no-link .#checks.aarch64-darwin.tests-1-of-4"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: tests-darwin-1-of-4-log
          path: ${{ runner.temp }}/tests-darwin-1-of-4.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  tests-darwin-2:
    name: Tests Darwin 2/4
    needs: build-darwin
    runs-on: macos-15
    timeout-minutes: 90
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Run test partition 2/4 on Darwin
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#checks.aarch64-darwin.tests-2-of-4 \
            2>&1 | tee "$RUNNER_TEMP/tests-darwin-2-of-4.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/tests-darwin-2-of-4.log" \
            --title "Tests Darwin 2/4" \
            --reproduce "nix build -L --no-link .#checks.aarch64-darwin.tests-2-of-4"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: tests-darwin-2-of-4-log
          path: ${{ runner.temp }}/tests-darwin-2-of-4.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  tests-darwin-3:
    name: Tests Darwin 3/4
    needs: build-darwin
    runs-on: macos-15
    timeout-minutes: 90
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Run test partition 3/4 on Darwin
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#checks.aarch64-darwin.tests-3-of-4 \
            2>&1 | tee "$RUNNER_TEMP/tests-darwin-3-of-4.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/tests-darwin-3-of-4.log" \
            --title "Tests Darwin 3/4" \
            --reproduce "nix build -L --no-link .#checks.aarch64-darwin.tests-3-of-4"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: tests-darwin-3-of-4-log
          path: ${{ runner.temp }}/tests-darwin-3-of-4.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  coverage-0:
    name: Coverage 0/2
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Run test partition 0/2 with coverage
        run: |
          set -o pipefail
          nix build -L \
            --out-link "$RUNNER_TEMP/coverage-result" \
            .#checks.x86_64-linux.coverage-0-of-2 \
            2>&1 | tee "$RUNNER_TEMP/coverage-0-of-2.log"

      - name: Prepare shard tracefiles
        run: |
          mkdir -p "$RUNNER_TEMP/coverage-shard"
          cp -aL "$RUNNER_TEMP/coverage-result/coverage.info" \
            "$RUNNER_TEMP/coverage-result/summary.txt" \
            "$RUNNER_TEMP/coverage-shard/"

      - name: Upload shard tracefiles
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-shard-0
          path: ${{ runner.temp }}/coverage-shard
          if-no-files-found: error
          retention-days: 7
          compression-level: 9

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/coverage-0-of-2.log" \
            --title "Coverage 0/2" \
            --reproduce "nix build -L -o result-coverage .#checks.x86_64-linux.coverage-0-of-2"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-0-of-2-log
          path: ${{ runner.temp }}/coverage-0-of-2.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  coverage-1:
    name: Coverage 1/2
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Run test partition 1/2 with coverage
        run: |
          set -o pipefail
          nix build -L \
            --out-link "$RUNNER_TEMP/coverage-result" \
            .#checks.x86_64-linux.coverage-1-of-2 \
            2>&1 | tee "$RUNNER_TEMP/coverage-1-of-2.log"

      - name: Prepare shard tracefiles
        run: |
          mkdir -p "$RUNNER_TEMP/coverage-shard"
          cp -aL "$RUNNER_TEMP/coverage-result/coverage.info" \
            "$RUNNER_TEMP/coverage-result/summary.txt" \
            "$RUNNER_TEMP/coverage-shard/"

      - name: Upload shard tracefiles
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-shard-1
          path: ${{ runner.temp }}/coverage-shard
          if-no-files-found: error
          retention-days: 7
          compression-level: 9

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/coverage-1-of-2.log" \
            --title "Coverage 1/2" \
            --reproduce "nix build -L -o result-coverage .#checks.x86_64-linux.coverage-1-of-2"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-1-of-2-log
          path: ${{ runner.temp }}/coverage-1-of-2.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  coverage:
    name: Coverage
    needs:
      - coverage-0
      - coverage-1
    permissions:
      contents: read
      id-token: write
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Download shard tracefiles
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: coverage-shard-*
          path: ${{ runner.temp }}/coverage-shards

      - name: Merge shard tracefiles
        run: |
          set -o pipefail
          report="$RUNNER_TEMP/coverage-report"
          mkdir -p "$report"
          python3 dev/merge_lcov.py \
            --output "$report/coverage.info" \
            --summary "$report/summary.txt" \
            "$RUNNER_TEMP"/coverage-shards/coverage-shard-*/coverage.info
          {
            echo "## Merged coverage"
            echo
            echo '```text'
            cat "$report/summary.txt"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Render browsable report
        run: |
          set -o pipefail
          report="$RUNNER_TEMP/coverage-report"
          nix shell --inputs-from . nixpkgs#lcov -c genhtml \
            --branch-coverage \
            --ignore-errors inconsistent,category,count,range,source \
            --output-directory "$report/html" \
            "$report/coverage.info"

      - name: Upload browsable coverage report
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-report
          path: ${{ runner.temp }}/coverage-report
          if-no-files-found: error
          retention-days: 14
          compression-level: 9

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@303a32d7a59b442fa8d48b6a1cc6825c09c847a5 # v7.1.1
        with:
          use_oidc: true
          files: ${{ runner.temp }}/coverage-report/coverage.info
          disable_search: true
          fail_ci_if_error: true
          name: linux-clang
          verbose: true

  tests-asan:
    name: Tests ASan
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Run full test suite with ASan
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#checks.x86_64-linux.tests-asan \
            2>&1 | tee "$RUNNER_TEMP/tests-asan.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/tests-asan.log" \
            --title "Tests ASan" \
            --reproduce "nix build -L --no-link .#checks.x86_64-linux.tests-asan"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: tests-asan-log
          path: ${{ runner.temp }}/tests-asan.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  tests-ubsan:
    name: Tests UBSan
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Run full test suite with UBSan
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#checks.x86_64-linux.tests-ubsan \
            2>&1 | tee "$RUNNER_TEMP/tests-ubsan.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/tests-ubsan.log" \
            --title "Tests UBSan" \
            --reproduce "nix build -L --no-link .#checks.x86_64-linux.tests-ubsan"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: tests-ubsan-log
          path: ${{ runner.temp }}/tests-ubsan.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9
